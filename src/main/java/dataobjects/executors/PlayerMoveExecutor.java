package dataobjects.executors;

import dataobjects.DataObject;
import dataobjects.Executable;
import dataobjects.PlayerMove;

public class PlayerMoveExecutor implements Executor{
    @Override
    public PlayerMove execute(Executable executable) {
        return null;
    }
}
