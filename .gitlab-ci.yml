stages:
  - test
  - build

run_tests:
  image: apache
  stage: test
  script:
    - ./mvnw test
  artifacts:
    reports:
      junit: target/surefire-reports/TEST-*.xml

build_app:
  image: apache
  stage: build
  script:
    - ./mvnw package
